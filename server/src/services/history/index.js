// #8 History & Receipt — read model over rides+events (ADR BD-DOCS-041 step 8).
// Skeleton until its cutover; receipts keep the BD-RIDE-HISTORY-D-01 no-recompute shape.
import { darkService } from '../_skeleton.js';

export default darkService('history', '#8 History & Receipt — dark in scaffold');
