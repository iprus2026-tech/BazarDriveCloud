// #6 Notification fan-out (BD-DOCS-036). Skeleton until its phase.
import { darkService } from '../_skeleton.js';

export default darkService('notifications', '#6 Notifications — dark in scaffold');
